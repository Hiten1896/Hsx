# Website guidance

- Keep the site in one HTML file with inline CSS and JavaScript.
- Use CSS Grid or Flexbox, an intentional palette, readable typography, and smooth transitions.
- Three.js is available via CDN for optional interactive 3D scenes.